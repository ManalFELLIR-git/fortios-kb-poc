id: system.npu-setting.prp
version: 7.6.7
source_presence:
  ansible: false
  terraform: false
  cli_reference: true
attributes_flat:
  prp_port_in:
    canonical:
      type: string
      max_length: 35
      description: Ingress port configured to allow the PRP trailer not be stripped off when the PRP packets come in. All
        of the traffic originating from these ports will always be sent to the host. Physical interface name.
      source_attribute: prp-port-in
    canonical_source: merged
    field_sources:
      type: cli_reference
      max_length: cli_reference
      description: cli_reference
      source_attribute: cli_reference
    evidence:
      cli_reference:
        type: string
        raw_type: string
        description: Ingress port configured to allow the PRP trailer not be stripped off when the PRP packets come in. All
          of the traffic originating from these ports will always be sent to the host. Physical interface name.
        max_length: 35
        source_attribute: prp-port-in
    sources:
    - cli_reference
  prp_port_out:
    canonical:
      type: string
      max_length: 35
      description: Egress port configured to allow the PRP trailer not be stripped off when the PRP packets go out. Physical
        interface name.
      source_attribute: prp-port-out
    canonical_source: merged
    field_sources:
      type: cli_reference
      max_length: cli_reference
      description: cli_reference
      source_attribute: cli_reference
    evidence:
      cli_reference:
        type: string
        raw_type: string
        description: Egress port configured to allow the PRP trailer not be stripped off when the PRP packets go out. Physical
          interface name.
        max_length: 35
        source_attribute: prp-port-out
    sources:
    - cli_reference
conflicts: []
